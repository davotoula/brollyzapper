package api

// RescueNoticeForTest is the message noteDoubleEncodingRescue logs, so the
// external test can find that line without re-typing it. A removal signal
// asserted against a re-typed copy of its own message is one that a reworded
// sentence silently switches off.
const RescueNoticeForTest = rescueNotice
