// Package lnd is the gRPC client for the node: TLS from the mounted cert,
// macaroon attached as a per-RPC credential, the single invoice subscription with
// settle_index resume, and (from P3) SendPaymentV2 (spec §6).
package lnd
